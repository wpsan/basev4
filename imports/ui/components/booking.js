import React from 'react';
import { Row, Col, ListGroupItem, Input, Button } from 'react-bootstrap';
//import { updateDocument, removeDocument } from '../../api/documents/methods.js';

export const BookingList = ({ document }) => (
  <ListGroupItem>
    <Row>
      <Col xs={ 12 } sm={ 12 }>
      
       
      </Col>
    </Row>
  </ListGroupItem>
  );